
// dynamic colors
export const BLACK = (options) => {
    return `rgba(1,1,1,${options.opacity})`;
}
export const OFF_BLACK = (options) => {
    return `rgba(6,19,45,${options.opacity})`; // not a cool color yet :)
}

export const LENGTH_UNIT = 10;

export const FOOTER_HEIGHT = 4 * LENGTH_UNIT;

// media
export const MOBILE = 375;
export const TABLET = 768;
export const DESKTOP = 1150;

//font size
export const TINY = 8;
export const SMALL = 12;
